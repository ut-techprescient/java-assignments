package org.example;

import org.example.arrays.ArrayOperations;
import org.example.employee.EmpOperation;
import org.example.encryption.EncryptionOperation;
import org.example.fileutils.FileOperation;
import org.example.matrix.MatrixOperations;
import org.example.scraping.ScrapePage;

import java.util.Scanner;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        // Press Opt+Enter with your caret at the highlighted text to see how
        // IntelliJ IDEA suggests fixing it.
//        NameInput obj = new NameInput();
//        obj.execute();

//        Calculator cal = new Calculator();
//        cal.execute();

//        WordCount wc = new WordCount();
//        wc.execute();

//        ArrayOperations arr = new ArrayOperations();
//        arr.execute();

//        Palindrome palin = new Palindrome();
//        palin.execute();

//        FileOperation fo = new FileOperation();
//        fo.execute();

//        EmpOperation emp = new EmpOperation();
//        emp.execute();

//        ScrapePage page = new ScrapePage();
//        page.execute();

//        EncryptionOperation encop = new EncryptionOperation();
//        encop.execute();

        MatrixOperations matOps = new MatrixOperations();
        matOps.execute();

    }
}